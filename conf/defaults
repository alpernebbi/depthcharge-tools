#
# This file is parsed with a very simple parser (i.e. it's not YAML).
# If multiple blocks exist for the same machine, values in the later
# block override those in the earlier block. Setting a key multiple
# times within a block appends to it with a space.
#

defaults:
  cmdline: quiet splash
  max-size: 33554432
  compress: none lz4 lzma

Google Kevin:
  dtb-name: rk3399-gru-kevin.dtb
