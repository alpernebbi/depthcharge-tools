#
# This file is parsed with a very simple parser (i.e. it's not YAML).
# Setting a key multiple times within a block appends to it with a
# space. Settings in later blocks override those in earlier blocks.
#

defaults:
  description: Debian with linux-image-%s
  max-size: 33554432
  compress: none lz4 lzma

Google Kevin:
  dtb-name: rk3399-gru-kevin.dtb
